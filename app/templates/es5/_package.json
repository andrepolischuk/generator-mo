{
  "name": "<%= name %>",
  "description": "<%= description %>",
  "version": "0.0.0",
  "keywords": [
    ""
  ],
  "repository": "<%= githubUsername %>/<%= name %>",
  "dependencies": {
    <% if (cli) { %>"commander": "^2.8.1"<% } %>
  },
  "devDependencies": {
    "mocha": "^2.2.5"
  },
  "main": "index.js",<% if (cli) { %>
  "bin": "cli.js",<% } %>
  "scripts": {
    "test": "mocha"
  },
  "author": "<%= githubName %> <<%= githubEmail %>>",
  "license": "MIT"
}
